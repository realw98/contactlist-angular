{
  "name": "contactlist-angular",
  "private": true,
  "version": "0.0.0",
  "description": "Example project for AngularJS",
  "repository": "https://github.com/realw98/contactlist-angular",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "update-deps": "npm update",
    "postupdate-deps": "bower update",

    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000 -c-1 ./app"
  }
}
